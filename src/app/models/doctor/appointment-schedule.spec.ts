import { AppointmentSchedule } from './appointment-schedule';

describe('AppointmentSchedule', () => {
  it('should create an instance', () => {
    expect(new AppointmentSchedule()).toBeTruthy();
  });
});
